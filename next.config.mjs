/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
      domains: ['www.trustmenclinic.com', "maindoagency.pythonanywhere.com"],
    },
  };
  
  export default nextConfig;
  